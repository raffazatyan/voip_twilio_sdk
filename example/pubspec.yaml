name: voip_twilio_sdk_example
description: Example app for voip_twilio_sdk plugin
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.10.3
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  voip_twilio_sdk:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

